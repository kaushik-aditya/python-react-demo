services:
  backend:
    build: ./backend
    env_file:
      - ./backend/envs/.env.dev # <-- switch this per environment
    volumes:
      - ./backend:/app
      - ./backend/logs:/app/logs
    ports:
      - "8000:8000"

  frontend:
    build: ./recipe_app_ui    
    volumes:
      - ./recipe_app_ui:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    depends_on:
      - backend
